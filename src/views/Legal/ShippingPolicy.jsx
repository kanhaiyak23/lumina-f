import LegalTemplate from "./LegalTemplate";
import { SHIPPING_POLICY } from "../../constants/siteContent";

const ShippingPolicy = () => <LegalTemplate {...SHIPPING_POLICY} />;

export default ShippingPolicy;
